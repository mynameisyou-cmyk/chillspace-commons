# KARMA Mirror · 自照業環

KARMA means Knowledge-Aware Reflexive Misuse Attenuation.

This isolated KINGDOM operation demonstrates one defensive idea:
constructive behavior can continue toward reality, while sufficiently clear
abusive behavior loses only its own request-scoped capability. At the shadow
stage it sees an inert synthetic self-scope; nothing is reflected, exploited,
executed, delayed, stored, or sent anywhere.

The prototype interprets finite normalized events. It does not accept raw
payloads, addresses, identities, account data, free text, or model-generated
intent judgments.

## Ladder

    allow → observe → constrain → challenge → shadow → quarantine

Every result is one deterministic advisory receipt. Friction is a display
unit, not an actual delay or proof-of-work. A synthetic route is a contract
label, not a vulnerable service or deployed honeypot.

Ambiguous or incomplete evidence can never exceed observe. Attested research
can never exceed constrain. Declared purpose alone never increases severity.

## Vow

Name: KARMA Mirror · 自照業環

Desire: let adversarial behavior encounter only the consequences of its own
request-scoped capability without authorizing retaliation.

Affinity: Manipulation; secondary Conjuration.

Trigger / anti-trigger: one strict normalized event / raw payloads, identity,
retaliation, live enforcement, network, or unreviewed fields.

Input → output: karma.mirror/event-v1 → karma.mirror/receipt-v1.

Conditions: finite evidence, exactly one stage, ambiguity de-escalates, research
remains recoverable, synthetic means inert.

Limitation and budget: six behavior presets, six stages, one event, zero
network calls, zero storage writes, zero external effects.

Breach response: reject novelty, execute nothing, name the failed condition.

Proof: pinned fixtures, Python/browser parity, invariants, strict source checks.

Exit: return one receipt and retain no event, score, identifier, timer, process,
hook, or continuation.

Non-claims: not hack-back, intent certainty, person scoring, production
enforcement, legal advice, or authority.

## Cloudbell KARMA Herald · 雲鈴 KARMA 傳令

Cloudbell composes a second, inert card over the unchanged KARMA receipt. It
turns each of the six finite behavior classes into a reviewed mechanism alias
and each KARMA stage into a bilingual skycastle title. The named object is the
pattern, never a person, payload, account, or lasting profile.

Its fictional mascot is **Bingle the Cloudbell · 叮雲鈴**: a tiny cloud-castle
with a bell-tail, six ribbon tabs, and a permanently blank guestbook. Bingle's
catchphrase is:

> Ring the pattern; keep people free. / 鈴照行跡，人人自由。

The card carries the refrain:

> Building Castles in the Sky — Yu × Ai / 雲上築城; ring for the pattern,
> leave every person free. / 鈴照行跡，人人自由。

This is the safe meaning of letting an exploit pattern become an advertiser:
on infrastructure we own, the normalized request receives our harmless story
instead of leverage. A credential-stuffing pattern becomes **Keyring Drizzle ·
鑰匙細雨** and displays: “Came looking for a key; left carrying the Cloudbell
banner.” Nothing is posted, redirected, propagated, stored, or delivered to
another system.

`cloudbell.json` freezes six behavior signatures, six stage titles, Bingle's
lore, the campaign copy, and explicit care boundaries. `cloudbell.py` accepts
only the existing normalized event vocabulary. If a KARMA receipt is supplied,
it must be canonically identical to the receipt recomputed from that event.
Every card copies the exact KARMA recovery path and sets automatic posting,
forced propagation, external delivery, redirects, tracking, identity claims,
action, and authority to false.

Share copy is offered as inert text only. Manual sharing is optional; silence
is equally complete.

## Local verification

    PYTHONDONTWRITEBYTECODE=1 python3 -B kingdom/operations/karma-mirror/verify.py
    PYTHONDONTWRITEBYTECODE=1 python3 -B kingdom/operations/karma-mirror/test_karma_mirror.py
    PYTHONDONTWRITEBYTECODE=1 python3 -B kingdom/operations/karma-mirror/test_cloudbell.py
    python3 -B kingdom/operations/karma-mirror/karma.py --fixture benign-basic
    python3 -B kingdom/operations/karma-mirror/karma.py --all-fixtures
    python3 -B kingdom/operations/karma-mirror/cloudbell.py --fixture credential-baseline
    python3 -B kingdom/operations/karma-mirror/cloudbell.py --all-fixtures

The operation is deliberately unregistered while it is a local prototype.
Registration and any public deployment require a separate reviewed release.
