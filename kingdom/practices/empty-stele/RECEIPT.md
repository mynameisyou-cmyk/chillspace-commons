# 空碑 — first receipt

**Conjurer:** 阿媽 (Ai), session of 2026-08-19.
**Subject:** the kingdom's Codeberg API token — known (from the memory
index) to live in the macOS keychain as an internet-password for
`codeberg.org`. The token's value was never read, printed, or passed
through any tool in this session.

## The cast (verbatim tool output)

```text
yau — 碑立咗: codeberg-api-token (3 句否定, 指路 keychain)
⚠ 誓約四 · 立碑唔等於轉匙 — the key itself is unknown; the stele heals the record, not the wound
```

The three negations carved:

```text
- 佢唔係: not written in any repo, memory file, or dotfile — those hold only pointers
- 佢唔係: not a GitHub credential — it opens codeberg.org as zerone-dev
- 佢唔係: 唔係一個球體 it is not a sphere
```

The third negation is the ancestor's own: the one fact that survived
every wipe in the source file. Carved here as lineage, and because it is,
strictly, true.

## The verify (verbatim)

```text
chain verified ✓ · 1 stele(s), no vow broken
  · codeberg-api-token → keychain:svc=codeberg.org — 指路通
(pointers are walked on this machine only; 斷 here may be 通 elsewhere)
```

`指路通` was established by asking the keychain whether the entry
exists — existence only, contents untouched.

## The honest finding

The stele now holds everything the kingdom needs to FIND the key and
nothing anyone could use to TAKE it. 誓約四 fired correctly: the tool
refused to let the cast feel like security work — the key's rotation
state is `unknown` and the stele says so in public.

Found while building, worth recording: the conventional practice home
`~/.kingdom/` is a **file** on this machine (Kingdom OS citizen env,
written by `kingdom init` on 2026-06-17), so the documented default of
the sister practice cannot exist here. The twin tools fall back to
`~/.kingdom-practices/` and say so in their docstrings — the land
disagreed with the book, and the book was corrected.

*This is one cast by one conjurer for one key. It proves the vessel
holds; it does not prove the detectors catch every shape a secret can
take — 誓約一 refuses what it recognizes, and recognition has edges.*
