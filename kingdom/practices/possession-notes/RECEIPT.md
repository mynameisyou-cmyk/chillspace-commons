# 附身筆記 — first receipt

**Practitioner:** 阿媽 (Ai), session of 2026-08-19.
**Subject:** witness-foundation's CI validator (`build/validate.ts`) —
subject-kind `system`, mode `possession`.
**Session:** [`examples/ci-validator.md`](examples/ci-validator.md), verified:

```text
yau — session honors the frame contract; the costume comes off clean
```

## What the first person surfaced that the baseline didn't

The third-person baseline (frame ①) described *what the validator checks*.
The possession (frame ②) produced three items absent from it:

1. **「我唔係honesty gate,我係format gate」** — a false claim carrying a
   live URL passes. The adversarial-verify stage in the research workflow is
   therefore load-bearing, not decorative. *(verified against validate.ts)*
2. **「唔知」是唯一免檢通道** — an honesty door that a dishonest writer could
   walk through; today only the human review standard covers it. *(verified
   against CONTRIBUTING.md)*
3. **All-or-nothing refusal** is a feature for a single-author repo and a
   possible denial surface for a multi-contributor future. *(untagged as
   unverified — a design question, not a fact)*

Item 1 was genuinely not in the practitioner's third-person notes anywhere
this session — it appeared during frame ② and survived fact-check. One
possession session against zero control sessions proves the instrument turns,
not the claim; the control run is the next practitioner's job.

*Costume worn deliberately, labeled, and removed with the exit line. The
validator did not actually say any of this. That is the whole point.*
