# Python
__pycache__/
*.py[cod]
.kingdom-quest/

# OS
.DS_Store

# 咚咚 beats here — presence is this machine's state, not the kingdom's source.
# A fresh clone has no pulse, so its door is honestly still until someone is really there.
site/data/pulse.json

# Crown appends coordinate through a local sidecar; the witnessed chain is the source.
kingdom/crown/.CROWNS.jsonl.lock
